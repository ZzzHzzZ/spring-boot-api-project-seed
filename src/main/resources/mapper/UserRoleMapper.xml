<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.company.project.dao.UserRoleMapper">
    <select id="listRoleByUser" resultType="java.lang.String">
        select role.rolename from t_user_role user_role
        left join t_role role on role.id = user_role.role_id
        where user_role.user_id = #{id}
    </select>
</mapper>